# Backend Setup Guide

Run the following commands in IDE terminal. Windows Powershell.

## 1. Virtual Environment Setup
cd backend
python -m venv venv
.\venv\Scripts\activate
pip install -r requirements.txt

## 2. Env Setup
cp .env.example .env